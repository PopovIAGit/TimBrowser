<UserControl x:Class="TimBrowser.Views.DownloadInformationModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <StackPanel Visibility="{Binding DownloadVisibility}">
        <TextBlock Text="{Binding ProgressString}" Margin="5,4,5,8" HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   FontSize="14" />
        <Button Content="Считать" Name="DownloadStart" Margin="4"
                Visibility="{Binding DownloadButtonVisibility}" Padding="32,8" VerticalAlignment="Center" HorizontalAlignment="Center"/>

    </StackPanel>
    
</UserControl>
