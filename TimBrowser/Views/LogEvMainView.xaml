<UserControl x:Class="TimBrowser.Views.LogEvMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid Background="White">
    	<Grid.ColumnDefinitions>
    		<ColumnDefinition Width="0.65*"/>
            <ColumnDefinition Width="0.35*"/>
    	</Grid.ColumnDefinitions>
        <ContentControl x:Name="LogEvEventsControl" />

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
            <RowDefinition Height="0.08*"/>
            <RowDefinition Height="0.92*"/>
        </Grid.RowDefinitions>
            
            <ContentControl x:Name="LogEvChosenParametersControl" Grid.Row="1" />           
            <TextBlock x:Name="TitleTextBlock" HorizontalAlignment="Right" Margin="4,8,12,8" TextWrapping="Wrap" Text="избранные параметры" d:LayoutOverrides="Height" FontSize="16"/>
            
        </Grid>
    </Grid>
    
</UserControl>
